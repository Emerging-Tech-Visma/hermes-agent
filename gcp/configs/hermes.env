# Appended to ~/.hermes/.env by 02-vm-install.sh
# No VERTEX_CREDENTIALS_PATH needed: the VM's attached service account
# provides Application Default Credentials.
# Only used if INSTALL_OPENVIKING=true (v1 memory stack); harmless otherwise.
OPENVIKING_ENDPOINT=http://localhost:1933
OPENVIKING_AGENT=hermes
